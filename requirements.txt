# Recruiting Bot Dependencies v2 - Force rebuild
# For Railway deployment

# Google APIs
google-api-python-client
google-auth
google-auth-httplib2
google-auth-oauthlib
google-genai

# Supabase
supabase

# Environment  
python-dotenv

# HTTP
requests
httpx

# Document parsing
python-docx

# LiveKit Agent (pin to 0.x — agent.py uses 0.x API)
livekit-agents[deepgram,google,silero,turn]>=0.12.0,<1.0.0
livekit-api>=0.7.0,<1.0.0
av>=10.0.0
